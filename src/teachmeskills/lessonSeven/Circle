public class Circle extends Figure {
    double radius;

    public Circle(double radius) {
        this.radius = radius;
    }
    public double getAreaCircle(){
         Math.PI * radius * radius;
    }
    public String getPerimeterCircle() {
        double perimeter = Math.PI * 2 *radius;
    }
